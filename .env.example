#
# Irado Chatbot / Dashboard - environment template
#
# Copy to .env (local) or manage these as Azure App Settings (recommended).
# NEVER commit real secrets to git.
#

# --- Database ---
POSTGRES_HOST=irado-chat-db.postgres.database.azure.com
POSTGRES_PORT=5432
POSTGRES_DB=irado_chat
POSTGRES_USER=irado_admin
POSTGRES_PASSWORD=
POSTGRES_SSLMODE=require

# Optional connection string formats (any one of these can be used)
# POSTGRES_CONNECTION_STRING=
# DATABASE_URL=

# --- App timezone ---
APP_TIMEZONE=Europe/Amsterdam
TZ=Europe/Amsterdam

# --- Basic Auth (chatbot API) ---
CHAT_BASIC_AUTH_USER=irado
CHAT_BASIC_AUTH_PASSWORD=

# --- Azure OpenAI ---
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_DEPLOYMENT=gpt-4o
AZURE_OPENAI_API_VERSION=2025-01-01-preview

# --- Dashboard (if needed) ---
WEBSITES_PORT=8000

